import React from 'react';

const TabIndexItems = function ( {tabInfo: {title, content}} ) {
  debugger;
  return (
    <div>
      <h1>{title}</h1>
      <p>{content}</p>
    </div>
  );
};

export default TabIndexItems;
